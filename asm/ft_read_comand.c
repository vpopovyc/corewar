#include "byte_code_header.h"

void	ft_read_comand(t_gamer *src)
{
	char *tmp;

	while (get_next_line(src->fd, tmp) > 0)
	{
		
	}
	// считать строку 
	// завалидоровать
	// если она относиться к текущему лейблу добавить в текущую ноду,
	// если новый лейбл создать новую ноду списка меток
	// 
}
